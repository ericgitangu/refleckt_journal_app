/** @type {import('next').NextConfig} */
const nextConfig = {
  // Official Next.js configuration options
  reactStrictMode: true,
  swcMinify: true,
  
  // Any necessary experimental options
  experimental: { }
};

module.exports = nextConfig; 