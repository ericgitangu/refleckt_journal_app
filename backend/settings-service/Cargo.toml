[package]
name = "journal-settings-service"
version = "0.1.0"
edition = "2021"

[dependencies]
# Use the consolidated common package
journal-common = { path = "../common" }

# All dependencies have been hoisted to journal-common
# This includes rust-bert and all AWS-related dependencies