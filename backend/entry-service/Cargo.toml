[package]
name = "journal-entry-service"
version = "0.1.0"
edition = "2021"

[dependencies]
# Use the consolidated common package
journal-common = { path = "../common" }

# All dependencies have been hoisted to journal-common
# This includes base64 and all AWS-related dependencies