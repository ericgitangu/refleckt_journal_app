[package]
name = "journal-analytics-service"
version = "0.1.0"
edition = "2021"

[dependencies]
# Use the consolidated common package
journal-common = { path = "../common" }

# All dependencies have been hoisted to journal-common