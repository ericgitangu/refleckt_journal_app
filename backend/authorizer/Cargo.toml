[package]
name = "journal-authorizer"
version = "0.1.0"
edition = "2021"

[dependencies]
# Use the consolidated common package which already includes all needed dependencies
journal-common = { path = "../common" }

# No need for duplicating dependencies that are already in common